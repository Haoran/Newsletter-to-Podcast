# Copy this file to `.env` and fill in your values.
# Load it in your shell with:
#   set -a; source .env; set +a

# OpenAI: used for LLM cleaning, audio-friendly rewrite, and OpenAI TTS
OPENAI_API_KEY=sk-...your-openai-key...

# If you use Google Cloud TTS (set tts.provider: gcp in config.yaml),
# you can provide credentials in one of two ways:

# Option A — Inline JSON (single line). Leave empty if not used.
# Example (minified JSON):
# GCP_TTS_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...",...}
GCP_TTS_SERVICE_ACCOUNT_JSON=

# Option B — Point to a local key file (absolute path)
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/gcp-tts-service-account.json

# Diffbot (optional, used as a free-tier fallback fetcher when direct fetch fails)
DIFFBOT_TOKEN=
